# railway.toml — Config-as-code for Railway (Nixpacks builder)
[build]
builder = "NIXPACKS"

[deploy]
# Railway exposes a $PORT env var automatically
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
